var dataTarget;

var newSite = {
    init: function (event) {
        this.bindEvents();
    },

    // Binds the Events to the elements on the site
    bindEvents: function () {

    }
};

jQuery(document).ready(function($) {
    newSite.init();
});